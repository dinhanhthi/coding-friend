{
  "name": "bench-cli",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "bench-cli": "dist/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
