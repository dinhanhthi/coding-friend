# bench-webapp

A minimal web application with an API client, in-memory cache, and input validators.

## Structure

- `src/lib/api.ts` — API client for user CRUD operations
- `src/lib/cache.ts` — In-memory cache with TTL support
- `src/lib/validator.ts` — Input validation functions

## Testing

```bash
npm test
```
