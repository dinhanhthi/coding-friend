## Checklist

- [ ] I ran the relevant tests and builds.
- [ ] If plugin source or generator code changed, I ran `npm run build:codex`.
- [ ] I ran `npm run verify:codex-drift` after staging generated changes.
- [ ] If I changed a forked Claude/Codex hook, I updated both implementations and their paired tests.
