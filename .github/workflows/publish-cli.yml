name: Publish CLI to npm

on:
  push:
    tags:
      - "cli-v*"

permissions:
  id-token: write # Required for OIDC
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cli
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # OIDC trusted publishing requires npm >= 11.5.1, but node 22 ships npm 10.9.x.
      # Pin to 11.11.0: last known-good before the 11.12+ self-upgrade regression
      # that drops `promise-retry` mid-upgrade and breaks subsequent `npm ci` calls.
      # Refs: nodejs/node#62425, npm/cli#9151
      - name: Upgrade npm
        run: npm install -g npm@11.11.0

      - name: Install dependencies
        run: npm ci

      - name: Publish to npm
        run: npm publish --provenance

  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "tag=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"

      - name: Extract changelog for this version
        id: changelog
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          VERSION="${TAG#cli-}"
          BODY=$(awk -v tag="$VERSION" '
            BEGIN { found=0 }
            /^## / {
              if (found) exit
              header=$2
              gsub(/[^v0-9.].*/, "", header)
              if (header == tag) { found=1; next }
            }
            found { print }
          ' cli/CHANGELOG.md)

          if [ -z "$BODY" ]; then
            echo "::warning::No changelog entry found for $VERSION, using default message"
            BODY="Release $TAG"
          fi

          echo "$BODY" > /tmp/release-body.md

      - name: Delete existing release if present
        env:
          TAG: ${{ steps.version.outputs.tag }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "$TAG" &>/dev/null; then
            echo "Deleting existing release for $TAG"
            gh release delete "$TAG" --yes
          fi

      - name: Create GitHub Release
        env:
          TAG: ${{ steps.version.outputs.tag }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "$TAG" \
            --title "$TAG" \
            --notes-file /tmp/release-body.md
