---
title: Quick Start
description: Your first 5 minutes with Coding Friend — learn the core workflow.
keywords: [quick start, getting started, tutorial, first steps]
---

Coding Friend enforces a disciplined engineering workflow. After installing the plugin, here are the steps to get started.

## 0. Scan your project into memory

```bash
# Enter claude code then run
/cf-scan
```

This will scan your project and extract the knowledge into the memory system. Read more about [`/cf-scan`](/docs/skills/cf-scan/) for more details.

## 1. Decide What to Build

```bash
/cf-advise Should we migrate to a monorepo or keep multiple repos?
```

Not sure yet whether — or which approach — is worth doing? `/cf-advise` interviews you one question at a time to surface the real requirements, then gives a verdict-first recommendation with pitfalls and ranked alternatives. It's advisory-only — it never writes code or a plan. Once you've decided, hand off to `/cf-plan` below. Check [`/cf-advise`](/docs/skills/cf-advise/) for more details.

## 2. Plan Your Work

```bash
/cf-plan Build a user authentication system

# Or even without /cf-plan (not recommended)
Build a user authentication system
```

Coding Friend explores your codebase, identifies existing patterns, and writes a structured implementation plan. The plan is saved to `docs/plans/` by default. Check [`/cf-plan`](/docs/skills/cf-plan/) for more details.

## 3. Write Code

Simply describe what you want to build. The [`cf-tdd`](/docs/skills/cf-tdd/) skill activates automatically and checks for `--add-tests` in your task to decide the mode. By default it uses **direct implementation** — code without new tests. To enable TDD, pass `--add-tests`:

```bash
/cf-plan --add-tests Build a user authentication system
```

With `--add-tests`, the skill enforces:

1. **RED** — Write a failing test first
2. **GREEN** — Write minimal code to pass
3. **REFACTOR** — Clean up while tests stay green

You can also enable TDD globally via `cf config` or `cf init` (sets `tdd: true` in config).

## 4. Review Your Code

```
/cf-review src/auth/
```

A multi-layer code review runs in a separate subagent, checking security, performance, style, and test coverage. Check [`/cf-review`](/docs/skills/cf-review/) for more details.

## 5. Fix Bugs

```
/cf-fix Login fails with 401 error
```

A bug fix workflow with problem verification. Check [`/cf-fix`](/docs/skills/cf-fix/) for more details.

## 6. Optimize Code

```
/cf-optimize getUserById query
```

A structured optimization workflow with before/after measurement. This skill also **auto-invokes** when you mention performance issues — just say "this endpoint is slow" and it activates. Check [`/cf-optimize`](/docs/skills/cf-optimize/) for more details.

## 7. Commit

```
/cf-commit Add user authentication
```

A smart conventional commit workflow with diff analysis. You should run it after you have written the code and verified that it works. Check [`/cf-commit`](/docs/skills/cf-commit/) for more details.

## 8. Ship It

```
/cf-ship Add user authentication
```

One command to verify all tests pass, create a conventional commit, push, and open a pull request. Check [`/cf-ship`](/docs/skills/cf-ship/) for more details.

## 9. Research

```
/cf-research GraphQL vs REST for mobile APIs
```

A deep research workflow with web search. The research is saved to `docs/research/` by default. Check [`/cf-research`](/docs/skills/cf-research/) for more details.

## 10. Capture Knowledge

```
/cf-remember auth flow
```

Capture project knowledge for the AI to use in future sessions. The knowledge is saved to `docs/memory/` by default. Check [`/cf-remember`](/docs/skills/cf-remember/) for more details.

## 11. Learn From Your Work

```
/cf-learn
```

Extract what you learned during the session into structured docs. Use [`/cf-teach`](/docs/skills/cf-teach/) for a deeper, conversational breakdown of what happened and why. Host them locally with [`cf learn host`](/docs/cli/cf-learn/) or integrate with other LLM clients via [`cf mcp`](/docs/cli/cf-mcp/). Check [`/cf-learn`](/docs/skills/cf-learn/) for more details.

<figure>
  <img src="/cf-host.png" alt="Learning Notes hosted locally" />
  <figcaption>
    Use `cf learn host` to run a local website at http://localhost:3333
  </figcaption>
</figure>

## 12. Ask Questions

```
/cf-ask How does the auth middleware work?
```

Ask questions about your codebase. The question is saved to `docs/memory/` by default. Check [`/cf-ask`](/docs/skills/cf-ask/) for more details.

## 13. Save Session for Another Machine

```
/cf-session refactor auth flow
```

Save the current conversation to `docs/sessions/` so you can resume it on another machine with `cf session load` + `claude --resume`. Check [`/cf-session`](/docs/skills/cf-session/) for more details.

## Tips

- Use [`/cf-help`](/docs/skills/cf-help/) to see all available commands or ask about skills, agents, or workflows (e.g. "/cf-help what skills are available?") or just ask naturally about Coding Friend (e.g. "how does cf-plan work?")
- Skills like [`cf-tdd`](/docs/skills/cf-tdd/), [`cf-verification`](/docs/skills/cf-verification/), and [`cf-optimize`](/docs/skills/cf-optimize/) activate automatically — no slash needed
