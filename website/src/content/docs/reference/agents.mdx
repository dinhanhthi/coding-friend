---
title: "Agents"
description: "6 specialized agents for different engineering tasks."
keywords:
  [agents, subagents, cf-explorer, cf-code-reviewer, cf-implementer, cf-planner]
---

## Overview

Coding Friend includes 6 specialized agents, each optimized for specific engineering workflows. Agents are automatically selected based on the skill you invoke.

## Agent Reference

| Agent              | Model         | Purpose                                                  |
| ------------------ | ------------- | -------------------------------------------------------- |
| `cf-explorer`      | Claude Haiku  | Read-only codebase exploration and context gathering     |
| `cf-code-reviewer` | Claude Opus   | Multi-layer code review with deep reasoning              |
| `cf-implementer`   | Claude Opus   | TDD-driven implementation with test-first approach       |
| `cf-planner`       | Inherited     | Task decomposition, approach brainstorming, and planning |
| `cf-writer`        | Claude Haiku  | Lightweight document writing and markdown generation     |
| `cf-writer-deep`   | Claude Sonnet | Deep reasoning for nuanced technical documentation       |

## When Each Agent Is Used

### cf-explorer

The foundational exploration agent used by multiple skills and agents. Runs on Claude Haiku for cost efficiency. Given a goal and specific questions, it maps project structure, searches for relevant files, reads code, and returns a structured report with findings, key files, dependencies, and code snippets.

Used by:

- [`/cf-plan`](/docs/skills/cf-plan/) — explores codebase before the cf-planner designs approaches
- [`/cf-ask`](/docs/skills/cf-ask/) — gathers code context to answer questions
- [`/cf-fix`](/docs/skills/cf-fix/) — explores code around a bug before diagnosing root cause

```
/cf-plan (clarify → explore → plan → approve)
    ↓ dispatch
cf-explorer (map → search → read → report)
    ↓ findings
cf-planner (analyze → design approaches → plan tasks)

/cf-ask (parse question)
    ↓ dispatch
cf-explorer (search → read → report)
    ↓ findings
cf-ask (synthesize answer → save to memory)

/cf-fix (understand bug → verify)
    ↓ dispatch
cf-explorer (trace code path → find relevant files → report)
    ↓ findings
cf-fix (diagnose root cause → confirm → dispatch cf-implementer)
```

### cf-code-reviewer

Invoked by [`/cf-review [target]`](/docs/skills/cf-review/). The agent handles **execution** — reading the diff, reading full files for context, and formatting the report.

The review **methodology** (4-layer checklist, security vulnerability table, confidence scoring) lives in [`cf-auto-review`](/docs/skills/cf-auto-review/), which the agent loads at review time. This keeps methodology in one place rather than duplicated across components.

```
/cf-review (workflow orchestration)
    ↓ fork + dispatch
cf-code-reviewer (execution — how to review)
    ↓ loads
cf-auto-review (methodology — what to check)
```

### cf-implementer

Dispatched by [`/cf-plan [task]`](/docs/skills/cf-plan/), [`/cf-fix [bug]`](/docs/skills/cf-fix/), and [`/cf-optimize [target]`](/docs/skills/cf-optimize/) for test-first implementation. Also dispatched by [`cf-tdd`](/docs/skills/cf-tdd/) for substantial implementations (3+ files).

The agent runs in an isolated context and follows strict RED → GREEN → REFACTOR discipline. Skills pass it a detailed prompt with task context, file paths, and test patterns, then review the agent's report.

```
/cf-plan (clarify → explore → plan → approve)
    ↓ dispatch per task
cf-implementer (RED → GREEN → REFACTOR → report) × N tasks

/cf-fix (diagnose → confirm approach)
    ↓ dispatch
cf-implementer (write regression test → fix → verify → report)

/cf-optimize (baseline → analyze → plan)
    ↓ dispatch
cf-implementer (write test → optimize → verify → report)

cf-tdd (auto-invoked for substantial work)
    ↓ dispatch
cf-implementer (RED → GREEN → REFACTOR → report)
```

### cf-planner

Invoked by [`/cf-plan [task]`](/docs/skills/cf-plan/) after the cf-explorer gathers codebase context. Takes the exploration report and designs 2-3 implementation approaches with pros, cons, effort, risk, and confidence levels. Recommends one approach and breaks it into sequential tasks.

### cf-writer

Used for quick document generation, skill documentation, and lightweight markdown files.

### cf-writer-deep

Invoked by `/cf-learn [topic]` with deep reasoning enabled. Extracts nuanced knowledge and creates comprehensive technical documentation.
